{
  "name": "pgaotcdn_cf_worker",
  "version": "1.0.0",
  "description": "A file upload service for Cloudflare Workers",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "build": "echo 'No build step required for Cloudflare Workers'",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "hono": "^3.12.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0",
    "wrangler": "^4.27.0"
  }
}
